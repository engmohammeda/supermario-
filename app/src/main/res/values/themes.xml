<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The framework theme on purpose: a Compose-only app needs no Material XML
         component library, and the dark background is what a bezel around a
         letterboxed NES frame wants anyway. -->
    <style name="Theme.MarioBox" parent="android:Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowTranslucentStatus">false</item>
    </style>
</resources>
