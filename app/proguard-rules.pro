# The engine module keeps its own JNI rules through consumer-rules.pro; these are
# the app-level additions, and there are deliberately none yet: nothing in :app is
# reached reflectively. Revisit if a library starts needing keeps.
